<shape xmlns:android="http://schemas.android.com/apk/res/android" >

    <gradient
        android:angle="270"
        android:centerColor="@color/gamepad_press_analog_center"
        android:endColor="@color/gamepad_press_analog_end"
        android:startColor="@color/gamepad_press_analog_start" />

    <stroke
        android:width="2dp"
        android:color="@color/gamepad_press_analog_border" />

    <corners android:radius="4dp" />

</shape>